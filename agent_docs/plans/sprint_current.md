Active case: `CASE_local_retrieval_artifact_contract_note`

Reason:
- The local Ollama baseline and planner JSON smoke are now in place.
- The next smallest pre-integration gap is retrieval artifact shape, not more runtime plumbing.
- This slice stays local, pays down implicit-contract debt, and sets up a cleaner handoff to the first `mirai` adapter case.
